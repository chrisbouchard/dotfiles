# Ignore directories that contain transient or easily recoverable data.
$HOME/.cache
$HOME/.cargo
$HOME/.cups
$HOME/.dropbox
$HOME/.dropbox-dist
$HOME/.gem
$HOME/.homesick
$HOME/.local/lib/python*
$HOME/.local/share/Trash
$HOME/.local/share/containers
$HOME/.local/share/nvim/site/pack/packer
$HOME/.npm
$HOME/.nvm
$HOME/.oh-my-zsh
$HOME/.rbenv
$HOME/.rustup
$HOME/.tmp
$HOME/.var
$HOME/.zoom
$HOME/Downloads
$HOME/Dropbox

# Prevent any accidental loops (though the one-filesystem flag should also
# prevent this).
$HOME/Restic

# Ignore large build directories
**/node_modules
**/target/debug/build
**/target/release/build

